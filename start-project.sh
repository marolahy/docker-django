# Create the 'hello-word' project
django-admin startproject spa
# Create a folder to host the future App with the name
'simple-app'.
mkdir -p app/spa
# Create the 'spa' App
django-admin startapp spa app/spa